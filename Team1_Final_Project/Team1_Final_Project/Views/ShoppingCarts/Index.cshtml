@model IEnumerable<Team1_Final_Project.Models.Purchases.ShoppingCart>

@{
    ViewBag.Title = "Index";
}

<h2>Shopping Cart</h2>

<h3>Songs</h3>

<table class="table">
    <tr>
        <th>
            Song Title
        </th>
        <th>
           Rating
        </th>
        <th>
            Artist
        </th>
        <th>
            Price
        </th>
    </tr>

@foreach (var item in Model)
{
    @foreach (var item2 in item.Songs)
    {
    <tr>
        <td>
            @item2.SongName
        </td>
        <td>
            @item2.SongArtists
        </td>
        <td>
            <!--Ratings -->
        </td>
        <td>
            @item2.SongPrice
        </td>
    </tr>
        }
    }
</table>

<h3>Albums</h3>

<table class="table">
    <tr>
        <th>
            Album Title
        </th>
        <th>
            Rating
        </th>
        <th>
            Artist
        </th>
        <th>
            Price
        </th>
    </tr>

    @foreach (var item in Model)
    {
        @foreach (var item2 in item.Albums)
        {
        <tr>
            <td>
                @item2.AlbumName
            </td>
            <td>
                @item2.AlbumArtists
            </td>
            <td>
                <!--Ratings -->
            </td>
            <td>
                @item2.AlbumPrice
            </td>
            <td>
                @Html.ActionLink("Delete", "Delete", new { id = item.ShoppingCartID })
            </td>
        </tr>
        }
    }
</table>